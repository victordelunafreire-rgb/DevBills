import Button from '../components/Button';

const Home = () => {
	return (
		<div>
			<h2>Home</h2>
			<Button variant="outline" isLoading>
				Clique em mim
			</Button>
		</div>
	);
};

export default Home;
